<%= hint('Write your new comment here') %>
<%= form_for ([@post, @postcomment]) do |f| %>
  <p>
    <%= f.label :body, "New Comment" %>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
